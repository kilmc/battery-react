import { keywords } from '../../atomic.config.js';

export const float = {
  props: { '': 'float' },
  mobileFirstValues: keywords(
    {
      left: 'left',
      right: 'right',
      'float-none': 'none'
    },
    ''
  )
};
